local colors = require("colors")

sbar.bar({
	height = 30,
	color = colors.bar.bg,
	padding_right = 2,
	padding_left = 2,
	blur_radius = 60,
	corner_radius = 20,
	y_offset = 2,
	font_smoothing = true,
})
